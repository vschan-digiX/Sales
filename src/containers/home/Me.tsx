import React, { useState } from 'react';
import {
  Dimensions,
  Platform,
  RefreshControl,
  View
} from 'react-native';
//  Native Base Components
import {
  useTheme,
  Box,
  HStack,
  VStack,
  Text,
  Button,
  Badge,
  Spacer,
  Pressable,
  ScrollView,
  Divider,
  FlatList,
} from 'native-base';
//Icons
import {Copy} from '../../assets/icons';

const Me = ({navigation}: {navigation: any}) => {


  return (
    <Box >
        <Text>Me</Text>
    </Box>
  );
};
export default Me;
